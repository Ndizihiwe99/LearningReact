import ListGroup from "./ListGroup";

export default ListGroup;

/*
function app(){
const [bugs, setbug]= useState([
{Id:1, type:'b1', fixed:'false'}
{Id:2, type:'b2', fixed:'false'}
])

const handleClick = () => {
//updating bugs
setbug(produce(draft=> {
const bug = draft.find(bug => bug === 1)
if(bug)
bug.fixed=true
}))
}

return 
(
<div onclick={handleClick}>click me</div>
)

}
*/
